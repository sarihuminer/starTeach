export class User {
    id: string;
    firstName: string;
    lastName: string;
    codeMosad: number;
    phone?: string;
    email?: string;

    // constructor(id: string, firstName: string, lastName: string, codeMosad: number, phone?: string, email?: string,) {

    // }
}